// Public surface of the Assessment Context Engine.
// Import only AssessmentContextService and its interface — sub-services are internal.
export { AssessmentContextService } from './assessmentContextService';
export type { IAssessmentContextService } from './interfaces';
